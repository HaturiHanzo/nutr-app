/**
 * Admin other controller
 *
 * @namespace Controllers
 */
(function () {
    'use strict';

    angular
        .module('nutr')
        .controller('adminOtherCtrl', [
            '$http',
            '$scope',
            function ($http, $scope) {
            }]);
}());