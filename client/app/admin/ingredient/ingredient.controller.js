/**
 * Admin ingredient controller
 *
 * @namespace Controllers
 */
(function () {
    'use strict';

    angular
        .module('nutr')
        .controller('adminIngredientCtrl', [
            '$http',
            '$scope',
            function ($http, $scope) {
            }]);
}());