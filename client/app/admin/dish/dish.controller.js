/**
 * Admin dish controller
 *
 * @namespace Controllers
 */
(function () {
    'use strict';

    angular
        .module('nutr')
        .controller('adminDishCtrl', [
            '$http',
            '$scope',
            function ($http, $scope) {
            }])
}());